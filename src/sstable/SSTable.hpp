#ifndef LSMDB_SSTABLE_HPP
#define LSMDB_SSTABLE_HPP

namespace lsmdb {

class SSTable {
    public:
        SSTable() = default;
        ~SSTable() = default;
};
}

#endif // LSMDB_SSTABLE_HPP